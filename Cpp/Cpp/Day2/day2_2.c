#include<stdio.h>
#include<stdlib.h>
int main(void)
{
    int num; //declaration of variable / identifier
    num=20; //defining variable num (assigning a value to variable num)
    int a=50; //declaring and defining on the same line (Initialization)

    printf("num = %d",num);
    num=70;
    printf("  num = %d  " ,num);
    return 0;
}
